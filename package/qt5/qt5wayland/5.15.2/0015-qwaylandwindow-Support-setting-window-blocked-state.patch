From a09193a6bcd466dfda8729306e817d6986fa53cb Mon Sep 17 00:00:00 2001
From: Jeffy Chen <jeffy.chen@rock-chips.com>
Date: Fri, 22 Oct 2021 17:18:40 +0800
Subject: [PATCH 15/16] qwaylandwindow: Support setting window blocked state

Support setting window blocked state through app_id, for example:
app_id = "flags=blocked"

Signed-off-by: Jeffy Chen <jeffy.chen@rock-chips.com>
---
 src/client/qwaylandwindow.cpp | 31 +++++++++++++++++++++++++++++++
 src/client/qwaylandwindow_p.h |  3 +++
 2 files changed, 34 insertions(+)

diff --git a/src/client/qwaylandwindow.cpp b/src/client/qwaylandwindow.cpp
index 88a3683..f73a7fe 100644
--- a/src/client/qwaylandwindow.cpp
+++ b/src/client/qwaylandwindow.cpp
@@ -509,6 +509,32 @@ void QWaylandWindow::setMask(const QRegion &mask)
     mSurface->commit();
 }
 
+bool QWaylandWindow::windowEvent(QEvent *event)
+{
+    switch (event->type()) {
+    case QEvent::WindowBlocked:
+        {
+            mBlocked = true;
+
+            /* Update blocked status */
+            setWindowFlags(window()->flags());
+        }
+        break;
+    case QEvent::WindowUnblocked:
+        {
+            mBlocked = false;
+
+            /* Update blocked status */
+            setWindowFlags(window()->flags());
+        }
+        break;
+    default:
+        break;
+    }
+
+    return QPlatformWindow::windowEvent(event);
+}
+
 void QWaylandWindow::applyConfigureWhenPossible()
 {
     QMutexLocker resizeLocker(&mResizeLock);
@@ -813,6 +839,11 @@ void QWaylandWindow::setWindowFlags(Qt::WindowFlags flags)
         // HACK: Set window flags through app id
         char s[128] = "flags=";
 
+        if (mBlocked)
+            strcat(s, "blocked|");
+        else
+            strcat(s, "-blocked|");
+
 #define SET_FLAG(flag, str) \
         if (flags & (flag)) strcat(s, str "|"); \
         else strcat(s, "-" str "|");
diff --git a/src/client/qwaylandwindow_p.h b/src/client/qwaylandwindow_p.h
index 01337cf..a6acb9d 100644
--- a/src/client/qwaylandwindow_p.h
+++ b/src/client/qwaylandwindow_p.h
@@ -154,6 +154,8 @@ public:
 
     void setMask(const QRegion &region) override;
 
+    bool windowEvent(QEvent *event) override;
+
     int scale() const;
     qreal devicePixelRatio() const override;
 
@@ -278,6 +280,7 @@ private:
     void handleScreensChanged();
     void sendRecursiveExposeEvent();
 
+    bool mBlocked = false;
     bool mInResizeFromApplyConfigure = false;
     bool lastVisible = false;
     QRect mLastExposeGeometry;
-- 
2.20.1

