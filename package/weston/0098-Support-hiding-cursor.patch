From 06aad269f82bbdd2f6bed41ead5d7cf8add299f5 Mon Sep 17 00:00:00 2001
From: Jeffy Chen <jeffy.chen@rock-chips.com>
Date: Tue, 19 Sep 2023 11:05:33 +0800
Subject: [PATCH 98/98] Support hiding cursor

Add below to weston.ini:
[shell]
hide-cursor=true

Signed-off-by: Jeffy Chen <jeffy.chen@rock-chips.com>
---
 compositor/main.c             | 2 ++
 include/libweston/libweston.h | 1 +
 libweston/compositor.c        | 3 +++
 3 files changed, 6 insertions(+)

diff --git a/compositor/main.c b/compositor/main.c
index 20af3d9..f77d060 100644
--- a/compositor/main.c
+++ b/compositor/main.c
@@ -4192,6 +4192,8 @@ wet_main(int argc, char *argv[], const struct weston_testsuite_data *test_data)
 	section = weston_config_get_section(config, "shell", NULL, NULL);
 	weston_config_section_get_int(section, "cursor-size",
 				      &wet.compositor->cursor_size, 0);
+	weston_config_section_get_bool(section, "hide-cursor",
+				       &wet.compositor->hide_cursor, false);
 
 	weston_compositor_log_capabilities(wet.compositor);
 
diff --git a/include/libweston/libweston.h b/include/libweston/libweston.h
index cbfec11..961e2c1 100644
--- a/include/libweston/libweston.h
+++ b/include/libweston/libweston.h
@@ -1533,6 +1533,7 @@ struct weston_compositor {
 	uint32_t pending_fade_out;
 
 	int cursor_size;
+	bool hide_cursor;
 };
 
 struct weston_solid_buffer_values {
diff --git a/libweston/compositor.c b/libweston/compositor.c
index f48c29a..8be6ef9 100644
--- a/libweston/compositor.c
+++ b/libweston/compositor.c
@@ -3080,6 +3080,9 @@ weston_compositor_build_view_list(struct weston_compositor *compositor,
 	wl_list_for_each(layer, &compositor->layer_list, link) {
 		bool system_layer = weston_compositor_is_system_layer(layer);
 
+		if (compositor->hide_cursor && layer == &compositor->cursor_layer)
+			continue;
+
 		wl_list_for_each(view, &layer->view_list.link, layer_link.link) {
 			if (compositor->warm_up && !system_layer) {
 				weston_log("seeing the first app\n");
-- 
2.20.1

