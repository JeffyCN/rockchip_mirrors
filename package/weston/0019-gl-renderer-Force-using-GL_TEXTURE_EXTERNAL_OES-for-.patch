From 487fe30d81adb3a25c7fa5da317a1529dcae16c7 Mon Sep 17 00:00:00 2001
From: Jeffy Chen <jeffy.chen@rock-chips.com>
Date: Wed, 23 Nov 2022 10:21:13 +0800
Subject: [PATCH 19/91] gl-renderer: Force using GL_TEXTURE_EXTERNAL_OES for
 imported dma-buf

Mali depends on that.

Signed-off-by: Jeffy Chen <jeffy.chen@rock-chips.com>
---
 libweston/renderer-gl/gl-renderer.c | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/libweston/renderer-gl/gl-renderer.c b/libweston/renderer-gl/gl-renderer.c
index aa8a49dcd..397019d2d 100644
--- a/libweston/renderer-gl/gl-renderer.c
+++ b/libweston/renderer-gl/gl-renderer.c
@@ -2882,6 +2882,10 @@ choose_texture_target(struct gl_renderer *gr,
 {
 	struct dmabuf_format *tmp, *format = NULL;
 
+	/* HACK: Mali needs GL_TEXTURE_EXTERNAL_OES for all imported dmabuf */
+	if (gr->is_mali_egl)
+		return GL_TEXTURE_EXTERNAL_OES;
+
 	wl_list_for_each(tmp, &gr->dmabuf_formats, link) {
 		if (tmp->format == attributes->format) {
 			format = tmp;
-- 
2.20.1

